import { type Office } from "./office.types";
import { type TypesAttention } from "./types_attention.types";
import { type Professor, ProfessorDefault } from "./professor.types";
import { type Student, StudentDefault } from "./student.types";
import { type Worker, WorkerDefault } from "./worker.types";
import { type External, ExternalDefault } from "./external.types";
import { type Role, type Permission, RoleDefault } from "./role.types";
import { type User, UserDefault } from "./user.types";

export {
  type Office,
  type TypesAttention,
  type Professor,
  ProfessorDefault,
  type Student,
  StudentDefault,
  type Worker,
  WorkerDefault,
  type External,
  ExternalDefault,
  type Role,
  type Permission,
  RoleDefault,
  type User,
  UserDefault,
};
